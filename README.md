# BillSync
A system that passively captures user activity (emails, documents, meetings), classifies it into structured legal work, and converts it into billable entries with an invoice-ready output compatible with systems like Ghost Practice.
